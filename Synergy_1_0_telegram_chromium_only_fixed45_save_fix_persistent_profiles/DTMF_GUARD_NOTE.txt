DTMF prompt-interrupt guard

This build preserves the fixed30 play_audio_in_tab() watcher-thread path that
restored mid-prompt DTMF interruption and Telegram debug logging.

Before changing backend/synergy.py play_audio_in_tab(), verify a live test call
still logs these lines when 1 is pressed during the initial prompt:
1. [audio][dtmf] Prompt listener started — watching for key during audio
2. [audio][dtmf] Thread saw key '1' during prompt
3. [audio][dtmf] Interrupting prompt for key '1'
4. [audio] Playing press1 audio after DTMF interrupt

If any line disappears, restore the guarded function before shipping.
